//Today's date
//
var currentDate = moment().format(dd MMMM Do YY);
$("#currentDay").html(currentDate);


//current time
 
//save button
var saveButton = $('saveBtn');
saveButton.on('click', function() {
    var hour = 
})
